export function AddEditPatientVisit(
    { data, mode = "add", handleCancel, onAddEdit }
) {
    return (
        <>
            <p>
                Add Edit PatientVisit
            </p>
        </>
    )
}